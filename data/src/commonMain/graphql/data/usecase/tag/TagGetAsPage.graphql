query GetTagsAsPage($pageInput: PageInput!) {
  getAllTagsAsPage(pageInput: $pageInput) {
    tags {
      id
      name
      usedInProducts
      createdAt
    }
    info {
      count
      pages
      prev
      next
    }
  }
}
