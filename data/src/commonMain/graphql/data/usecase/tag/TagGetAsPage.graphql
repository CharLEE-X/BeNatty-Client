query GetTagsAsPage($pageInput: PageInput!) {
  getTagsAsPage(pageInput: $pageInput) {
    tags {
      id
      name
      createdAt
    }
    info {
      count
      pages
      prev
      next
    }
  }
}
