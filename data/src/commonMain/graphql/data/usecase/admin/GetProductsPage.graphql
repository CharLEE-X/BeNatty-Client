query GetProductsPage($pageInput: PageInput!) {
  getProductsPage(pageInput: $pageInput) {
    info {
      count
      next
      pages
      prev
    }
    products {
      id
      name
    }
  }
}
