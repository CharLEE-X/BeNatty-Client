mutation LandingConfigRemoveCollageItem($configId: String!, $collageItemId: String!) {
  landingConfigRemoveCollageItem(configId: $configId, collageItemId: $collageItemId) {
    companyInfo {
      contactInfo {
        email
        phone
      }
      openingTimes {
        close
        dayFrom
        dayTo
        open
      }
    }
    id
    landingConfig {
      collageItems {
        alt
        description
        id
        imageUrl
        title
      }
    }
  }
}
