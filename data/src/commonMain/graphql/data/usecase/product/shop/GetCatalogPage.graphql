query GetCatalogPage($catalogPageInput: CatalogPageInput!) {
  getCatalogPage(catalogPageInput: $catalogPageInput) {
    info {
      count
      next
      pages
      prev
    }
    products {
      id
      media {
        alt
        id
        mediaType
        url
      }
      price
      title
    }
  }
}
