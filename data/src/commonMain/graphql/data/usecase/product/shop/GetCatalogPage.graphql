query GetCatalogPage($input: CatalogPageInput!) {
  getCatalogPage(input: $input) {
    info {
      count
      next
      pages
      prev
    }
    products {
      id
      name
      currentPrice
      sizes
      media {
        keyName
        url
        alt
        type
      }
    }
  }
}
