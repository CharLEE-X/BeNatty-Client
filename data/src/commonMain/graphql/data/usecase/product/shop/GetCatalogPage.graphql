query GetCatalogPage($catalogPageInput: CatalogPageInput!) {
  getCatalogPage(input: $catalogPageInput) {
    info {
      count
      next
      pages
      prev
    }
    products {
      id
      media {
        keyName
        url
        alt
        type
      }
      price
      title
    }
  }
}
