query GetCatalogPage($input: CatalogPageInput!) {
  getCatalogPage(input: $input) {
    info {
      count
      next
      pages
      prev
    }
    products {
      id
      media {
        keyName
        url
        alt
        type
      }
      regularPrice
      salePrice
      name
    }
  }
}
